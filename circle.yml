general:
  branches:
    only:
      - master

machine:
  timezone: Asia/Tokyo
  node:
    version: 6

test:
  override:
    - npm run build

deployment:
  publish:
    branch: master
    commands:
      - git config --global user.name "Circle CI"
      - git config --global user.email "yuhei@ryden.co.jp"
      - npm run deploy
